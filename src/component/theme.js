const themeColor = "#3378dd";

const errorColor = "#fae6e3";
const successColor = "#dff2ef";
const warnColor = "#fff6e4";
const infoColor = themeColor;
const whiteColor = "#ffffff";

const THEME = {
  themeColor,
  errorColor,
  successColor,
  warnColor,
  infoColor: infoColor,
  whiteColor
};

export { THEME };
